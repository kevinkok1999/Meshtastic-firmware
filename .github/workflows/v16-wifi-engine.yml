name: V16 Wi-Fi Engine Superseded

on:
  push:
    branches:
      - v16-wifi-engine
    paths:
      - ".github/workflows/v16-wifi-engine.yml"

permissions:
  contents: read

concurrency:
  group: meshoffgridnl-firmware-build
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Superseded by v16-wifi-connection-engine; no firmware is published from this branch."
